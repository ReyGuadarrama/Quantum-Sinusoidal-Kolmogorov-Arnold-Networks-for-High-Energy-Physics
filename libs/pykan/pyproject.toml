[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pykan"
version = "0.1.0"
description = "Librería KAN modificada"
dependencies = ["torch", "numpy", "sympy", "tqdm", "pyyaml"]

[tool.setuptools.packages.find]
where = ["."]
include = ["kan*"]
